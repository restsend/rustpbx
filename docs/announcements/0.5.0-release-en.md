# RustPBX 0.5.0

> Single-binary, programmable SIP PBX in Rust — a modern alternative for FreeSWITCH/Asterisk users and voice-agent developers.

**1,874 commits · 13 contributors · 708 files changed** since 0.4.6 (June – September 2026). Read the [Breaking Changes](#breaking-changes--upgrade-guide) before upgrading. Full details: [detailed changelog](0.5.0-changelog-en.md) · 中文公告: [0.5.0-release-zh.md](0.5.0-release-zh.md)

---

## Highlights

**Media engine rewrite** — ~6.4 ms call setup (2.7× faster) · zero-copy fast-path relay at ~0.14% core / ~0.57 MB per channel · native WebRTC↔SIP interop: 800 cross-transport relays and 500 Opus↔PCMU transcodes at **0% packet loss** · 18,000-call soak with flat memory and no audio glitches.

**5,000 concurrent calls with full recording on < 4 cores** — CPS=200 storm + stereo recording + MySQL CDR: 4,943 concurrent recorded calls at ≈3.9 cores, **0.00% packet loss**, database nearly idle (2–4 QPS), 4,980 stereo WAVs (4.4 GB) with zero drops. Rule of thumb: ≈ 0.08 core per recorded channel.

**Queue & ACD in the community edition** — skill-group routing and escalation plans · concurrent agent dialing · supervisor `listen` / `whisper` / `barge` / `takeover` · agent sign-in/out, wrap-up, queue-entry tracing.

**RWI control plane for voice agents** — record from early media · REFER blind-transfer with B2BUA fallback · `voip_bridge:` bidirectional PCM streaming · conference `create/add/remove/mute/end/merge` · `call.transfer.replace` (RFC 3891) · `dtmf.collect` · `app.chain`.

**Live transcription & predictive outbound** — `GET /cc/calls/{call_id}/transcript` SSE with per-leg ASR (Deepgram-compatible protocol, lazy start, media path untouched) · local SenseVoice offline transcription · `POST /ami/v1/outbound/dial` with SSE event stream and `on_answer` hooks.

**Clusters, containers, graceful shutdown** — distributed session registry with request auto-forwarding · SIGTERM drain-then-exit · recordings/CDR/signaling to S3, sessions in shared PostgreSQL/MySQL — add replicas to scale.

---

## What's New

- **Media**: Opus + G.729 codecs, 48 kHz telephone-event DTMF; video SDP negotiation, re-INVITE and passthrough; per-leg negotiated media profiles
- **Queue/CC**: RFC 7433 UUI call correlation; 3-way conference RTP bridging and conference room dial-in; queues/IVR/trunks/routes/ACL storable in the database and editable from the console
- **IVR**: unified `StartApp` orchestration with builtin flows; step-mode IVR driven by an external HTTP provider; session-level fallback recovery; mid-call recording segments
- **Cluster**: media anchored to the call-owning node; cluster-aware AMI — `GET /calls` active-call query, session lookup, log viewer, remote reload
- **RWI**: originate out a named carrier trunk with agent attribution; root call identity and enriched broadcast events on all call-scoped events
- **SipFlow/Recording**: high-throughput ingest (MTU batching, SSRC loss reports); deterministic flush so post-call queries see tail messages; CDR/JSONL upload to S3/HTTP; waveform playback in the console
- **Console**: call-trace timeline and unified error-code reference; bilingual UI (EN/中文); routing strategy stack with runtime ordering and PATCH API; built-in web dialer and JsSIP softphone
- **Auth**: OAuth2-style SSO login broker; JWT fast REGISTER (no 401/407 round-trip); in-dialog auth caching; AMI CIDR allowlists
- **Routing/Trunks**: `parallel_fork` + `max_ring_time`; weighted trunk selection; OPTIONS health checks; per-trunk `external_ip`/`bind_ip` with network profiles; per-trunk CPS and concurrency limiters
- **Transfers**: interactive transfers on resilient media bridges; configurable call forwarding; queue transfers carry UUI context to agents
- **NAT**: `sip_external_ip`, `auto_sip_external_ip`, `local_networks`, `[[network_profile]]`; standardized SIP error codes with call-trace events
- **Ops**: hot reload for ACL/trunks/routes/queues/IVR/TLS; console diagnostics toolbox with route evaluator; `/metrics` (Prometheus) + `/healthz`

---

## Performance

- 5k-CC recording collapse fixed (`7c37af8c`): 17.7% packet loss at 3.3k concurrent → 4,943 concurrent recorded calls at 0% loss
- SIP signaling and RTP media on isolated tokio runtimes (`7b82ba88`); ~130 hot-path heap allocations removed (`c72bf042`)
- Memory locator on `DashMap`; SipFlow ingest/flusher throughput + jemalloc by default (`49262229`, `f0429cb0`, `c841228f`)
- Faster proxy failover: earlier 100 Trying and no-trying timeout (`3c005717`)

Full benchmarks: [tests/bench/bench.md](../../tests/bench/bench.md) · [docs/capacity-planning.md](../capacity-planning.md)

---

## Notable Fixes

- **NAT**: inbound Contact rewritten so ACK/BYE route behind NAT (`310c2224`); Contact URI derived from inbound transaction for WebRTC registrations (`f2465792`, #251)
- **re-INVITE**: B2BUA relay with refreshed SDP snapshots (`03cdfa2e`); bridge rebuild and codec renegotiation (`dfeee606`, `94b01d0e`, #183)
- **DTMF**: first DTMF and first inbound packets no longer dropped (`746ac8cd`); duplicate events eliminated (`5f1e6b49`, #158)
- **CDR**: missing ring time and incorrect durations (`4967c057`); `status_code` locked to the INVITE final response (`58d15aed`)
- **WebRTC**: cluster-routed BYE swallowed by `.invalid` addresses (`5075d5d4`); one-way audio from unstripped RTP header extensions (`a2fd4ff2`); callee WS BYE black hole (`fdd624a1`)
- **Registration**: agent lifecycle preserved across registration races (`a7cd2cff`); presence ownership survives restarts (`065d50b0`)
- **Queue**: agent logout no longer sticks the panel on idle (`7f26b911`); `queue_joined` strictly ordered before ACD events (`53055bd0`)
- **Media**: PCMA↔G722 one-way audio (`963e4d88`); G.722 decoding/jitter (`2994b59f`); RTP continuity across bridge segments and transfers (`335fb933`, `840e4b09`)
- **SIP core**: orphan BYE answered 200 instead of hanging the peer (`d927c332`, #245); immediate 487 on CANCEL (`5dc10a70`); session-timer refresher role (`edd81a47`)
- **Leaks**: WebRTC/P2P per-call media (`19ff5184`, `6b6f2663`); RWI originate/transfer dialog guards (`0953126a`); REFER NOTIFY listeners (`fec6c2b8`)

---

## Breaking Changes & Upgrade Guide

### Recording & SipFlow
- **`[sipflow]` alone no longer captures RTP.** RTP capture now requires an explicit recording config:

  ```toml
  [recording]
  enabled = true
  type = "sipflow"   # signaling-only capture is unchanged
  ```
- `callrecord` storage now defaults to `type = "database"` (previously local JSON files in examples). Configure `[callrecord]` explicitly for local files, S3, or HTTP upload.
- Recording auto-start point is configurable (early media vs answer).

### SIP behavior
- `session_timer` now defaults to **`true`** (RFC 4028, Supported mode);
- `video_codecs` defaults to `["H264", "VP8"]`; new `video_policy` option (`pass_through` / `strip` / `transcode`);
- New NAT/network controls: `sip_external_ip`, `auto_sip_external_ip`, `local_networks`, `[[network_profile]]` — review your NAT setup if you relied on implicit defaults.

### RWI protocol (the biggest changes)
- Event rename: **`call_incoming` → `call_created`**;
- Removed commands: `media.stream_start` / `media.stream_stop` / `inject_start` / `inject_stop` — use `call.transfer` to a **`voip_bridge:`/`bridge:` WebSocket endpoint** (binary PCM16 frames) for real-time audio; `record.mask_segment` removed;
- Removed events: `dn_state_changed` / `dn_registered` / `dn_unregistered`, `call_metadata_updated`, `ivr_flow_transitioned`, `queue_overflowed`, `parallel_originate_*`, `session_resumed`, `media_stream_started`/`stopped`, `record.segment_masked`;
- **`record_stopped` no longer carries the recording URL** — listen for `record_end` / `recording_metadata_available`;
- Renamed event: `supervisor.takeover.completed` → `supervisor.takeover.started`;
- Parameter changes: `call.originate` (no `hold_music`/`hold_music_target`), `queue.enqueue` (no `skills`/`max_wait_secs`), `conference.create` (no `backend`/`record`);
- Event field changes: `ani`/`dnis`/`root_call_id`/`operation_id` removed; `caller`/`caller_name`/`callee_name`/`trace_id`/`hangup_by`/`duration_secs`/`transfer_target` added;
- Config keys `[rwi].enabled` and `[rwi].contexts` removed;
- `[rwi.smart_routing]`, `[rwi.local_rules]`, `[rwi.sip_header_passthrough]` are now explicitly documented as **design proposals, not implemented** — don't rely on them.

### Console / HTTP API
- All console JSON endpoints are unified under the **`/api` prefix** behind an auth middleware with a CSRF guard; new static Bearer token auth via `[console].api_tokens` with scopes;
- The `/licenses` list route was removed (`/licenses/verify` remains);
- Locator/registration payloads changed: `aor` is now a full `IP:port` address, `destination` carries a transport prefix, and new fields were added (`home_proxy`, `supports_webrtc`, `transport`, `user_agent`, `expires`).

### Removed components & config
- Addons removed: `endpoint_manager`, `enterprise_auth`, `sbc`;
- Config keys removed: `[proxy].sip_flow_max_items`, `media_cache_path`;
- `verify-i18n` binary removed; new `rustpbx dump` subcommand exports the DB DDL;
- The `opus` Cargo feature was removed — Opus is always available via audio-codec.

### Deployment
- **Source builds now require submodules**: `git clone --recurse-submodules` (addons `cc`, `wholesale`, `ivr_editor`, `telemetry`, `voicemail` are submodules now);
- Docker image: base moved to `debian:trixie-slim`, `libopus0` no longer needed, sounds moved to `/app/config/sounds` (a compat symlink at `/app/sounds` is kept);
- New environment variables: `DEEPGRAM_API_KEY` (remote transcription), `CC_PHONE_AUTH_SECRET` (phone auth).

### Database
- Migrations run automatically on startup (disable with `--skip-migrate`); this release adds three: `outbound_sip_trunk_id` column on call records, `config_entries` table (DB-backed config), `cluster_sessions` table (cluster session registry);
- Stale migration records are pruned and self-healed at startup;
- Stack upgrades: sqlx 0.8 → 0.9, sea-orm 1.1 → 2.0.

### Dependencies
| Crate | 0.4.6 | 0.5.0 |
|---|---|---|
| rsipstack | 0.5.12 | **0.6.7** |
| rustrtc | 0.3.66 | **0.3.132** |
| audio-codec | 0.3.38 | **0.4.5** |
| sea-orm | 1.1.20 | **2.0** |
| sqlx | 0.8.6 | **0.9.0** |

---

## New Contributors

Thanks to everyone who contributed code, reviews, and bug reports. First-time contributors in this release:

- **autopeasant**
- **fluffy1781** (as Eugene Wolffe / Cream Crumpets) — Contact URI fixes (#240)
- **v0l** (Kieran) — SDES-SRTP negotiation (#227) and queue-target fix
- **Tyson George** — originate out a named carrier trunk

Continuing from previous releases: jinti (maintainer), yeoleobun, elliottjiang, liu zhen, Gonzalo Ruiz, zhouhailin, davidcforbes, deweydb, Umran Hussain — and everyone who filed issues and tested release candidates.

---

## Upgrading

```bash
docker pull ghcr.io/restsend/rustpbx:latest
# or from source:
git clone --recurse-submodules https://github.com/restsend/rustpbx.git
```

Review the [Breaking Changes](#breaking-changes--upgrade-guide) above — in particular the RWI event renames and the recording configuration — then start the new binary; database migrations run automatically.

- Documentation: [docs/](../../README.md) · [Configuration Guide](../configuration.md)
- Community announcement (中文): [0.5.0-release-zh.md](0.5.0-release-zh.md)
- Docker: `ghcr.io/restsend/rustpbx:latest` · Web console: `http://localhost:8080/console/`

## Full Changelog

https://github.com/restsend/rustpbx/compare/0.4.6...0.5.0
